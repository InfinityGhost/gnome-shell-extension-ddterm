[tox]
skipsdist = true
envlist = py3

[testenv]
deps =
    pytest
    pytest-xdist
    pytest-html
    ansi2html
    PyGObject
    wand
commands = python -m pytest --html=report.html --junitxml=junit.xml {posargs}
